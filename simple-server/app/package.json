{
  "name": "simple-server",
  "version": "1.0.0",
  "description": "simple-server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1",
    "winston": "^3.2.1"
  }
}
